# Audit summary — 04_EXAMBUILDER_PASSAGES

Generated by `npm run audit:stored-exams`. See `audit-report.json` for full detail.

## Scope

- `library/pool-seed/` (6 files)
- `library/{de,en,es}/{level}/exams/` (static library exams)
- `data/exams/`, `data/demo/`

## Headline (post-fix)

| Metric | Value |
|--------|-------|
| Files scanned | 85 |
| Exams audited | 98 |
| Needs curation | 50 |
| `passage_missing` | **0** |
| `transcript_missing` | **0** |

## Root cause fixed

Before: ExamBuilder used `passage?.text || q.question` for layout items → reading parts showed question text (~52 words) or empty transcripts.

After: PassageResolver resolves `passageId` (including sibling inference) and embeds full passage/transcript in output schema.

## Remaining issues (flagged, not fabricated)

1. **`passage_too_short`** — bank passages below level minimum (B1 min 150w; longest in pool often 37–75w).
2. **`item_count_mismatch`** — question bank smaller than blueprint `questionsTotal` (e.g. lesen teil 2 expects 8–10, bank provides 4).
3. **`part_missing`** — modules not yet assembled (Cambridge `use_of_english`, some DELE writing/speaking).

All affected pool entries carry `needsCuration: true` with `curationReasons[]` after `npm run seed:pool`.

## Commands

```bash
npm run repair:passage-ids   # one-off bank passageId repair
npm run seed:pool            # regenerate pool-seed
npm run audit:stored-exams   # refresh this report
npm run test:library         # includes passage regression test
```
